#dataframe example

no = c(1, 2, 3, 4)
name = c("apple", "peach", "banana", "grape")
price = c(500, 200, 120, 50)
qty = c(5, 4, 3, 2)
sales = data.frame(NO = no, NAME = name, PR = price, Q = qty)
sales


